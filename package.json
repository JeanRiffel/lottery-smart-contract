{
  "scripts": {
    "check": "node -e \"console.log('Hello from my script!')\"",
    "build": "truffle compile",
    "migrate": "truffle migrate"
  },

  "dependencies": {
    "solc": "^0.8.0"
  }
}
